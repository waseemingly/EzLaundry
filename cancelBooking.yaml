appId: com.example.app
---
#start from homepage
- tapOn:
    point: "50%,55%"
#cancel a booking only when there are current bookings 
- runFlow:
    when:
      notVisible: "You have no bookings."
    file: "deleteBooking.yaml"